## Boltz-2 Evaluation

First, run `pip install boltz[cuda] -U`.
You can use the MSA generated by Protenix.

### 1. Construct Input FASTA
Run `python input_construct.py`.

### 2. Structure Prediction
Run `python structure_prediction.py`.

### 3. TM-Score Calculation
Run `python cal_tmscore.py`.
